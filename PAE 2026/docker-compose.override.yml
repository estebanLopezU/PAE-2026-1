version: '3.8'

services:
  postgres:
    ports:
      - "5432:5432"

  backend:
    environment:
      DEBUG: "true"
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
